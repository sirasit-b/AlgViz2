# ── OS / system ────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini

# ── Editors / IDE ──────────────────────────────
.vscode/
.idea/
*.sublime-*
*.swp
*.swo
*~

# ── Node (build script has no deps; guard anyway) ──
node_modules/
npm-debug.log*
yarn-error.log*
.pnpm-debug.log*

# ── PHP ────────────────────────────────────────
vendor/
composer.lock
.phpunit.result.cache

# ── Logs / local dev scratch ───────────────────
*.log
.php-server.log

# NOTE: build outputs (assets/*, config/topics.php, preview.html) ARE committed
# on purpose so the folder is clone-and-deploy. Re-run `node build/build.mjs`
# after editing anything in src/.
